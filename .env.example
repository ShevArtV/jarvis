TELEGRAM_TOKEN=your_token_here
ALLOWED_USER_IDS=comma,separated,user,ids

# Дефолтный движок LLM для новых топиков: claude (дефолт), codex или opencode.
# Существующие топики хранят свой движок в БД; переключить активный топик можно
# командой /engine <name> в самом Telegram (новый session_id под новый движок,
# cwd сохраняется, контекст прежнего диалога не переносится).
# JARVIS_ENGINE=claude

# Бинарники и таймауты — по умолчанию ищутся в PATH.
# CLAUDE_BIN=claude
# CLAUDE_TIMEOUT=3600
# CODEX_BIN=codex
# CODEX_TIMEOUT=3600
# OPENCODE_BIN=opencode
# OPENCODE_TIMEOUT=3600

# Опционально для codex: дефолтная модель и список моделей для /engine UI.
# Если CODEX_MODELS не задан, Jarvis берёт список из ~/.codex/models_cache.json,
# затем падает обратно на встроенный список известных Codex-моделей.
# CODEX_MODEL=gpt-5.5
# CODEX_MODELS=gpt-5.5,gpt-5.4,gpt-5.4-mini,gpt-5.3-codex,gpt-5.2

# Опционально для opencode: если не заданы, берутся из opencode config.
# OPENCODE_MODEL=anthropic/claude-sonnet-4-5
# OPENCODE_AGENT=build
# OPENCODE_VARIANT=high

# Playwright MCP для всех движков подключается автоматически при активации движка.
# По умолчанию Jarvis подключает Playwright MCP к CDP endpoint `chrome`.
# JARVIS_PLAYWRIGHT_MCP=1
# PLAYWRIGHT_MCP_MODE=cdp
# PLAYWRIGHT_MCP_CDP_ENDPOINT=chrome
# PLAYWRIGHT_MCP_NPX=/home/shevartv/.nvm/versions/node/v22.22.0/bin/npx
# PLAYWRIGHT_MCP_PACKAGE=@playwright/mcp@latest
# PLAYWRIGHT_MCP_ARGS=--caps=vision,pdf,devtools

# Дефолтный cwd для топиков без /bind.
# CLAUDE_CWD=/home/shevartv
